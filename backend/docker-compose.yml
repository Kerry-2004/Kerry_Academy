services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kerryht_academy
    ports:
      - "5432:5432"
    volumes:
      - kerryht_academy_pgdata:/var/lib/postgresql/data

volumes:
  kerryht_academy_pgdata:
