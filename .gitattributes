# Normalise les fins de ligne. Les fichiers exécutés côté serveur Linux
# doivent impérativement garder des fins de ligne LF (sinon bash/systemd/nginx
# peuvent échouer à cause des caractères \r de Windows).
* text=auto

*.sh    text eol=lf
*.service text eol=lf
*.conf  text eol=lf
